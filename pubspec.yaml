name: my_chat_app
description: Sample chat app using Supabase and Flutter

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  dart_openai: ^1.9.1
  dio: ^5.1.1
  supabase_flutter: ^1.6.0
  timeago: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
